package org.example.projet_pi.entity;

public enum PaymentFrequency {
    MONTHLY,      // Paiement chaque mois
    SEMI_ANNUAL,  // Paiement tous les 6 mois
    ANNUAL        // Paiement une fois par an
}